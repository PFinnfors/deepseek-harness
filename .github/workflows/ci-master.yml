# DeepSeek runner drills and benchmarks are disabled in this personal fork.
# Use fork-ci.yml for master and pull-request validation.
name: Upstream master CI (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/fork-ci.yml"
