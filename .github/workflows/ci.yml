# Upstream CI is retained in Git history but disabled in this personal fork.
# Use fork-ci.yml for routine validation.
name: Upstream CI (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/fork-ci.yml"
